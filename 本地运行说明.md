# 本地运行说明

## 启动与停止

双击本目录下的 启动.command 即可，它会自动挂载开发磁盘映像并拉起前后端。停止双击 停止.command。

首次编译约需一分钟，终端窗口关闭不影响后台服务。

## 访问地址

本机浏览器打开 http://localhost:8001

同一局域网的其他电脑打开 http://192.168.1.212:8001

后端接口文档 http://localhost:8000/docs

## 登录账号

当前前端仍使用 Ant Design Pro 自带的演示数据，账号为 admin 或 user，密码统一 ant.design。

后端已准备好真实账号，接入前端之后启用，密码统一 123456。

| 账号 | 身份 |
|------|------|
| admin | 系统管理员 |
| editor | 直属编辑主管 |
| hr | 人事主管 |
| service | 客服主管 |
| delivery | 交付对接老师 |

## 代码位置

前端在 /Volumes/PaidanDev/admin，后端在 /Volumes/PaidanDev/server，本目录下的 admin 与 server 是指向它们的快捷方式。

PaidanDev 是一块APFS格式的磁盘映像，映像文件本身存放在移动硬盘根目录，名为 PaidanDev.sparsebundle，按实际用量增长，当前约2G，上限100G。

## 为什么要用磁盘映像

主盘460G仅剩几百兆可用，装不下前端依赖。移动硬盘空间充足但格式为exFAT，实测有三个致命问题。一是同一份依赖在exFAT上占用39G，在APFS上只占1.9G。二是macOS在exFAT上生成的隐藏文件会破坏git索引。三是Node工具链在含中文的路径下解析配置会失败。

磁盘映像同时解决这三点，映像存在移动硬盘上不占主盘，内部是标准APFS，挂载路径为纯英文的 /Volumes/PaidanDev。

移动硬盘拔出前建议先执行 停止.command。重新插入后双击 启动.command 会自动重新挂载。

## 技术栈

前端为 Ant Design Pro 6.0.3，React加UmiJS，打包器为umi新版的utoo。

后端为 FastAPI，目前实现登录、取用户信息、刷新令牌三个接口，采用内存存储，重启后令牌失效。业务接口与数据库尚未接入。

## 已知问题

Ant Design Pro 6.0.3 的 @utoo/pack 主包在依赖解析时会漏装，已手工补装。若将来重新执行 npm install 后启动报 Cannot find module '@utoo/pack/package.json'，重新执行 npm i -D @utoo/pack 即可。

本机curl访问本地端口需要加 --noproxy 参数，浏览器若打不开请检查代理软件是否绕过本地地址。

## 当前进度

前端为 Ant Design Pro 原版骨架，尚未做任何业务改造，界面与官方演示站一致。

## 下一步

确认界面风格后进入业务开发，包括员工档案、四类角色的评分页、权重计算看板、人工调权后台与月度快照查询，同时把前端登录切到后端真实账号。
