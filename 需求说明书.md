# 派单权重综合评价系统 需求说明书

版本 v0.1 ｜ 编写日期 2026-09-17 ｜ 状态 待业务方确认

---

## 1. 背景与目标

目前派单依赖主管的主观印象与临时沟通，缺少可追溯、可比较的量化依据。本系统把分散在直属编辑主管、人事主管、客服主管、交付部对接老师手中的评价，与好评率、退款率等业务数据合并，每月产出一份可直接驱动派单的员工权重表。

系统要达成四件事。第一，把四类评价人的打分按各自专业领域收口，谁最懂哪块就只评哪块。第二，主观评分与客观数据按固定公式合成单一权重分，公式对全员一致、对管理层透明。第三，权重按月度周期滚动更新，形成历史快照，可回溯任一月份的派单依据。第四，保留后台人工干预通道，对离职交接、待淘汰、违纪等特殊情形直接调降或冻结权重。

全部数据存放于公司局域网服务器，不经过任何外部云服务。

---

## 2. 角色与职责边界

| 角色 | 系统权限 | 评价领域 | 角色权重（建议） |
|------|---------|---------|----------------|
| 直属编辑主管 | 评分端 | 交稿质量、专业能力、修改配合、交付及时性 | 0.45 |
| 交付部对接老师 | 评分端 | 客户反馈、沟通响应、返稿处理 | 0.25 |
| 客服主管 | 评分端 | 服务态度、沟通规范、投诉情况 | 0.18 |
| 人事主管 | 评分端 | 工作状态、出勤稳定、团队配合、纪律 | 0.12 |
| 系统管理员 | 管理端全权 | 不参与打分，负责调权、配置、导数 | 不适用 |
| 被评员工 | 只读或不可见 | 不适用 | 不适用 |

职责边界的原则是互不越界。人事主管看不到交稿质量题目，交付对接老师看不到出勤纪律题目，每个评价人登录后只能看到属于自己领域的题项。一名员工可能由多位直属主管带，此时该角色权重在多位主管间均分。

角色权重可在管理端配置，上表为初始建议值，四项之和须等于1。直属编辑主管份额最高，反映业务判断最贴近交付结果这一事实。

---

## 3. 评价维度与题项设计

### 3.1 主观评分部分

每个题项采用1至5分制，附分档说明，避免评价人凭感觉打分。

| 评价人 | 题项 | 题项权重 | 评分锚点示例 |
|--------|------|---------|-------------|
| 直属编辑主管 | 交稿质量 | 0.35 | 5分为几乎无需返修，1分为频繁整体重写 |
| 直属编辑主管 | 专业能力 | 0.25 | 覆盖学科匹配度、文献处理、方法规范 |
| 直属编辑主管 | 修改配合度 | 0.20 | 对修改意见的响应速度与落实程度 |
| 直属编辑主管 | 交付及时性 | 0.20 | 是否按约定节点交稿 |
| 交付对接老师 | 客户满意反馈 | 0.40 | 来自客户端的主观评价汇总 |
| 交付对接老师 | 沟通响应速度 | 0.30 | 对接消息的回复时效 |
| 交付对接老师 | 返稿处理表现 | 0.30 | 返稿后的态度与完成度 |
| 客服主管 | 服务态度 | 0.40 | 与客户沟通中的礼貌与耐心 |
| 客服主管 | 沟通规范 | 0.30 | 是否遵守话术与信息安全红线 |
| 客服主管 | 投诉情况 | 0.30 | 有效投诉次数的主观归因 |
| 人事主管 | 工作状态 | 0.35 | 精神面貌、任务承接意愿 |
| 人事主管 | 出勤与在线稳定 | 0.30 | 请假、失联、响应时段 |
| 人事主管 | 团队配合 | 0.20 | 协作意愿、互助表现 |
| 人事主管 | 纪律遵守 | 0.15 | 制度执行、私单风险 |

评分时必须填写一句以上文字说明，低于3分的题项强制填写具体事例，用于后续申诉与复核。

### 3.2 客观数据部分

| 指标 | 方向 | 计算口径 | 指标权重（建议） |
|------|------|---------|----------------|
| 好评率 | 正向 | 客户好评单数 ÷ 已评价单数 | 0.30 |
| 退款率 | 负向 | 因该员工原因退款单数 ÷ 交付单数 | 0.25 |
| 返稿率 | 负向 | 发生返稿的单数 ÷ 交付单数 | 0.20 |
| 按时交付率 | 正向 | 按约定节点交付单数 ÷ 交付单数 | 0.15 |
| 有效投诉次数 | 负向 | 客服记录并核实的投诉件数 | 0.10 |

客观数据在首期通过Excel或CSV导入，字段与现有业务系统导出格式对齐，后续预留HTTP接口直连派单系统数据库。当月交付单数低于设定阈值（建议5单）时，该员工客观分不参与计算，权重全部落在主观评分上，并在权重表中标记数据不足。

---

## 4. 权重计算算法

### 4.1 打分标准化

不同评价人的打分尺度差异明显，有人习惯普遍给4至5分，有人压分严格。直接加权会让宽松评委手下的员工系统性占优。因此在合成之前，对每位评价人当期的全部打分做标准化处理。

设评价人r对员工i的原始加权得分为 x_ri，该评价人当期所有打分的均值为 μ_r、标准差为 σ_r，则

    z_ri = (x_ri − μ_r) / σ_r
    s_ri = clip(75 + 8 × z_ri, 0, 100)

当σ_r小于0.3，即该评价人几乎给所有人打同一分数时，跳过标准化直接线性映射至百分制，并向管理端推送区分度不足告警。

### 4.2 主观分合成

    S_subj = Σ_r ( w_r × s_ri )

w_r为角色权重。若某评价人当期未提交，其权重按剩余角色的相对比例重新分配，不留空档。当直属编辑主管未提交时，本期权重不出具，系统转为沿用上月值并置为待补录状态。

### 4.3 客观分合成

每个指标先做全员分位数归一化，负向指标取反，再按指标权重加权，得到百分制的 S_obj。分位数归一化可避免极端值拉扯整体分布。

### 4.4 原始权重与月度平滑

    W_raw = 0.60 × S_subj + 0.40 × S_obj
    W_smooth = 0.70 × W_raw(本月) + 0.30 × W_final(上月)

平滑项的作用是防止单月偶发事件导致派单量剧烈震荡。新入职员工前两个月无历史值，直接取 W_raw，并设置保护下限65分，保证新人有基本单量以完成能力观察。

### 4.5 人工调整与最终权重

    W_final = clip( W_smooth × k + d, lower, upper )

k为乘数系数，d为加减分，二者均由管理端设置且默认为1与0。冻结状态直接令 W_final 等于0，该员工不进入派单池。

### 4.6 权重到派单的映射

| 档位 | W_final区间 | 派单策略 |
|------|------------|---------|
| S | 90至100 | 优先分配，可承接高价与高难度稿件 |
| A | 80至89 | 优先分配常规稿件 |
| B | 70至79 | 正常分配 |
| C | 60至69 | 限量分配，进入观察名单 |
| D | 60以下 | 暂停分配，进入淘汰或转岗流程 |

派单系统按 W_final 归一化后做加权随机或配额分配，同时受在手单量上限约束，避免高权重员工被压垮。具体消费方式由派单侧决定，本系统只负责输出权重与档位。

---

## 5. 人工调权机制

管理端支持四种干预方式。乘数调整适用于阶段性降权，例如待淘汰人员设为0.5。加减分适用于一次性奖惩。绝对覆盖适用于直接指定某人本月权重值。冻结适用于离职交接期与停职期间，权重归零且退出派单池。

每一次调整必须填写原因、生效周期与失效时间，系统记录操作人与操作时间，不可删除，只能追加撤销记录。调整仅对指定周期生效，到期自动回归算法值，避免临时措施长期遗留。

管理端提供调权前后对比预览，展示该操作对全员排名与档位分布的影响，确认后才落库。

---

## 6. 功能模块清单

评分端提供待评名单、分领域题项、草稿保存、批量提交与提交后锁定。评价人只能看到自己领域的题目与自己提交的历史记录，看不到其他角色的打分。

管理端涵盖员工档案、评价关系维护、权重配置、周期管理、客观数据导入、人工调权、权重发布与历史快照查询。角色权重、题项权重、指标权重、平滑系数、档位阈值全部可配置，改动记录留痕。

计算引擎按周期触发，执行标准化、合成、平滑、调权、分档五步，产出权重快照并冻结，快照一经发布不可修改，如需更正须发起新版本并说明原因。

数据看板展示全员权重分布、档位人数、环比变化、异常波动名单以及各评价人的打分区分度，供管理层复核。

导出与对接支持权重表导出Excel，并提供只读HTTP接口供派单系统按工号查询当期权重与档位。

---

## 7. 周期与流程

评价周期为自然月。每月25日系统开放评分入口并推送待办提醒，次月3日24时关闭。次月4日计算引擎自动跑批产出草稿版权重，管理员在4日至5日完成复核与人工调权，5日发布正式权重，当月6日起派单系统按新权重执行。

逾期未提交的评价人由系统记录并通知其上级，缺评按第4.2节规则处理。

---

## 8. 部署与运维要求

部署在公司局域网服务器，采用Docker Compose编排，包含应用服务、数据库与反向代理三个容器。员工通过内网IP或内网域名访问，不开放公网端口。数据库每日自动备份至同一局域网内的另一台设备，保留90天。

账号体系独立建设，支持按角色授权。若后续需要与企业微信或钉钉打通扫码登录，作为二期扩展考虑，一期采用账号密码加强制改初始密码。

系统需支持10至200名员工规模，单周期打分总量在数千条量级，普通台式机即可承载。

---

## 9. 安全与公平性约束

评分数据对被评员工默认不可见，管理端可配置为仅展示档位不展示具体分数。评价人之间互相不可见打分内容，仅管理员可见全量。

系统对以下情形自动告警。某评价人打分标准差过低，提示区分度不足。某员工权重环比波动超过15分，提示核查。某评价人对特定员工连续三个周期打分显著偏离其他角色，提示可能存在个人偏见。

全部打分记录、调权记录、配置变更记录写入审计日志，保留不少于三年，支持按人、按周期检索。

---

## 10. 待确认事项

第一，好评率、退款率、返稿率的原始数据当前存放在何处，能否直接导出，字段口径是否已有统一定义。这决定客观数据是手工导入还是接口直连。

第二，一名员工是否可能同时归属多位直属编辑主管，若存在，是按均分还是按主带主管加权。

第三，是否需要引入员工专长标签，例如学科方向、稿件类型、语种，用于派单时的匹配度加成。本次需求中的领域划分指评价人的评价领域，专长标签属于另一维度，可作为二期扩展。

第四，被评员工是否有权查看自己的权重与档位，是否开放申诉通道，申诉由谁受理。

第五，权重是否直接决定派单数量，还是仅作为派单系统的输入因子之一，派单侧是否还有在手单量、擅长领域、客户指定等约束。

第六，历史数据是否需要回溯计算，若需要，可追溯的最早月份是哪个。
