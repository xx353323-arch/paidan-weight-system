# 派单权重综合评价系统 部署文档

面向负责部署的开发工程师。系统为前后端分离架构，后端 FastAPI 提供接口，前端 Ant Design Pro 构建成静态文件，数据库为 SQLite 单文件。

仓库地址 https://github.com/xx353323-arch/paidan-weight-system

---

## 一、环境要求

| 项目 | 要求 |
|------|------|
| 操作系统 | Linux，推荐 Ubuntu 22.04 或 CentOS 8 以上 |
| 内存 | 2G 起步，构建前端时峰值约 3G，建议 4G |
| 磁盘 | 5G，其中前端构建依赖约 2G，可在构建后清理 |
| 对外端口 | 一个 HTTP 端口即可，默认 8080，前端与接口同域 |

使用人数在两百人以内，单机足够。数据库是 SQLite 单文件，无需额外的数据库服务。

---

## 二、方式一，Docker 部署（推荐）

需要 Docker 20.10 以上与 Docker Compose V2。

### 首次部署

```bash
git clone https://github.com/xx353323-arch/paidan-weight-system.git
cd paidan-weight-system

cp .env.example .env
# 编辑 .env，必须修改 PAIDAN_JWT_SECRET，用下面的命令生成
openssl rand -base64 48

docker compose up -d --build
```

构建前端约需五到十分钟。启动后访问 `http://服务器地址:8080`。

首次启动会自动建表并写入初始数据，包括 19 个员工账号与算法配置，无需手动初始化。

### 迭代更新

```bash
cd paidan-weight-system
git pull
docker compose up -d --build
```

数据库挂载在宿主机的 `deploy-data/` 目录，重新构建不会丢数据。

### 常用操作

```bash
docker compose logs -f server     # 看后端日志
docker compose logs -f web        # 看前端与 Nginx 日志
docker compose restart server     # 重启后端
docker compose down               # 停止全部服务
```

---

## 三、方式二，直接部署（服务器不能用 Docker 时）

需要 Python 3.12 以上、Node 22 以上、Nginx。

### 后端

```bash
git clone https://github.com/xx353323-arch/paidan-weight-system.git /opt/paidan
cd /opt/paidan/server

python3.12 -m venv .venv
.venv/bin/pip install -r requirements.txt

cat > .env <<'ENV'
PAIDAN_JWT_SECRET=这里填 openssl rand -base64 48 生成的字符串
PAIDAN_DB_PATH=/opt/paidan/server/data/paidan.db
PAIDAN_PUBLIC_FACING=true
ENV
```

用 systemd 守护进程，新建 `/etc/systemd/system/paidan.service`：

```ini
[Unit]
Description=Paidan Weight System API
After=network.target

[Service]
Type=simple
User=www-data
WorkingDirectory=/opt/paidan/server
EnvironmentFile=/opt/paidan/server/.env
ExecStart=/opt/paidan/server/.venv/bin/uvicorn app.main:app --host 127.0.0.1 --port 8000
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
```

```bash
systemctl daemon-reload && systemctl enable --now paidan
```

注意后端只监听 `127.0.0.1`，由 Nginx 反向代理对外，不要直接暴露 8000 端口。

**不要启用多 worker**。SQLite 在并发写入时会产生锁冲突，单进程配合 WAL 模式对两百人以内的规模完全够用。

### 前端

```bash
cd /opt/paidan/admin
npm ci
npm run build
# 产物在 dist/ 目录
```

构建完成后 `node_modules` 可以删除以释放空间，下次更新时重新安装。

### Nginx 配置

```nginx
server {
    listen 80;
    server_name paidan.公司域名.com;
    root /opt/paidan/admin/dist;
    index index.html;

    location /api/ {
        proxy_pass http://127.0.0.1:8000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 120s;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

`try_files` 那行是前端路由必需的，缺了会导致刷新页面出现 404。

### 迭代更新

```bash
cd /opt/paidan
git pull
cd server && .venv/bin/pip install -r requirements.txt && systemctl restart paidan
cd ../admin && npm ci && npm run build
```

---

## 四、外网访问的安全要求

系统会在公网暴露，以下几项必须做。

### 必须配置 HTTPS

用 Let's Encrypt 免费证书：

```bash
certbot --nginx -d paidan.公司域名.com
```

配置完成后在后端 `.env` 里设置 `PAIDAN_PUBLIC_FACING=true`。这个开关会启用 HSTS 响应头，同时关闭 `/docs` 接口文档页，避免接口结构对外暴露。

### 已内置的安全机制

代码层面已实现以下防护，无需额外配置：

连续输错密码 5 次锁定账号 15 分钟，锁定期间正确密码也无法登录，全部失败尝试写入审计日志并记录来源 IP。修改密码时校验强度，要求至少 8 位、同时包含字母和数字、不能与账号相同、不能是常见弱密码、不能重复字符过多。所有响应带 `X-Content-Type-Options`、`X-Frame-Options`、`Referrer-Policy`、`Permissions-Policy` 安全头。访问令牌有效期 12 小时，刷新令牌单次使用后轮换。

锁定阈值与密码策略可通过环境变量调整，见 `server/app/core/config.py`。

### 上线前必须做的三件事

第一，所有账号的初始密码等于账号名（例如账号 `xiaoxin` 密码 `xiaoxin`），系统会强制首次登录修改。**上线通知必须要求所有人当天完成首次登录改密**，否则初始密码在公网上等同于没有密码。

第二，`server/.env` 里的 `PAIDAN_JWT_SECRET` 必须改成随机值。这个密钥用于签发登录令牌，用默认值等于任何人都能伪造登录状态。

第三，确认 CORS 配置。如果前端和接口不同域，需要在 `.env` 里设置 `PAIDAN_CORS_ORIGINS`，只填实际使用的域名，不要用通配符。同域部署则无需配置。

### 建议补充

条件允许时在 Nginx 层加登录接口限流：

```nginx
limit_req_zone $binary_remote_addr zone=login:10m rate=10r/m;

location /api/auth/login {
    limit_req zone=login burst=5 nodelay;
    proxy_pass http://127.0.0.1:8000;
}
```

这一层挡住的是分布式扫号，应用层的账号锁定挡的是针对单个账号的爆破，两者互补。

---

## 五、数据备份

数据库是单文件，位置取决于部署方式：Docker 方式在 `deploy-data/paidan.db`，直接部署在 `/opt/paidan/server/data/paidan.db`。

**关键：SQLite 使用 WAL 模式，直接复制 .db 文件会丢失尚未落盘的数据。** 必须用 SQLite 自带的备份命令，或者连同 `-wal`、`-shm` 文件一起复制。

推荐的备份脚本，加进 crontab 每天执行：

```bash
#!/bin/bash
DB=/opt/paidan/server/data/paidan.db
DEST=/backup/paidan
mkdir -p $DEST
sqlite3 $DB ".backup '$DEST/paidan-$(date +%Y%m%d).db'"
find $DEST -name "paidan-*.db" -mtime +90 -delete
```

数据量很小，每天一份保留 90 天也只占几十兆。

每月发布权重结果后建议额外做一次备份并异地留存，那是当月派单的依据，需要可追溯。

---

## 六、故障排查

**页面能打开但接口全部报错** 检查后端是否在运行（`docker compose ps` 或 `systemctl status paidan`），以及 Nginx 的 `/api/` 反向代理地址是否正确。

**刷新页面出现 404** Nginx 缺少 `try_files $uri $uri/ /index.html;`。

**登录提示账号被锁定** 连续输错密码触发的保护，等 15 分钟自动解锁。需要立即解锁则在服务器上执行 `server/重置密码.sh 账号名`，该脚本会同时清除锁定状态。

**前端构建失败提示内存不足** 服务器内存不够，临时加 swap 或在本地构建后把 `dist/` 上传到服务器。

**时间显示不对** 容器时区已设为 `Asia/Shanghai`。直接部署时确认服务器时区正确，系统记录的时间取自服务器本地时间。

---

## 七、运维脚本

```bash
server/重置密码.sh <账号>      # 重置指定账号密码为账号名，同时解除锁定
server/重置系统.sh --yes       # 清空所有评价数据恢复初始状态，保留人员与配置
```

第二个脚本用于测试完毕转正式使用时清场，会删除全部打分与权重结果，保留员工档案、标签、主管归属和算法配置。执行前请确认已备份。

---

## 八、需要业务方确认的事

部署完成后，系统管理员账号是 `xiaoxin`（小欣），她可以在界面上管理员工档案、开启评价周期、执行跑批和发布结果。另有一个 `test` 测试账号，正式上线前建议停用。

评价周期需要管理员手动开启，开启后系统会自动给每位评价人生成待评名单。

---

## 九、编辑查看排名

每位编辑都有自己的账号，用户名为姓名拼音，初始密码与用户名相同，首次登录强制修改。

编辑登录后只有一个菜单「派单权重榜」，路径 `/board`。页面展示本组与其他组的完整排名，但**不显示任何姓名**，只有名次、权重分和档位。系统根据登录身份自动识别本人，在自己那一行标注「我」，并在页面顶部单独展示本人的组内名次、分数、档位和领先人数。

这样设计的原因是：早期版本做过免登录的公开榜单加姓名查询，但那样任何人把名单挨个查一遍就能还原出完整对应关系，匿名形同虚设。改成登录制后，编辑只能看到自己的身份信息，看不到任何其他人的姓名。

编辑账号的权限是系统里最低的，实测只能访问排名榜和获取自身信息两个接口，员工名单、标签、周期、权重明细、打分、调权、跑批记录全部拒绝访问。页面层面也只有排名榜一个入口，工作台和其余功能菜单都不会出现。

如果某位编辑本期没有参与评价（比如新入职还没被评过），登录后页面会提示并照常展示各组排名，不会报错。
